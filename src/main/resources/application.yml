spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/exxdx
    username: exxdx
    password: exxdxpw
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate.format_sql: true
  flyway:
    enabled: true

server:
  port: 8080